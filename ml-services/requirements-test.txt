# Lightweight requirements for CI test runs
# Heavy dependencies (torch, transformers, prophet, spacy, mlflow) are
# mocked in conftest.py and do NOT need to be installed for tests.

pytest==7.4.4
pytest-cov==4.1.0
pytest-asyncio==0.23.3

# Core data / ML (fast to install, actually used in test assertions)
numpy==1.26.3
pandas==2.2.0
scikit-learn==1.4.0
xgboost==2.0.3
joblib==1.3.2
scipy==1.12.0

# API / serialization
fastapi==0.109.2
pydantic==2.6.1
pydantic-settings==2.1.0
httpx==0.26.0

# Infrastructure stubs (lightweight)
redis==5.0.1
psutil==5.9.8
prometheus-client==0.19.0
