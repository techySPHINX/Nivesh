/**
 * Nivesh Design System — Theme Barrel Export
 */

export { palette, lightTheme, darkTheme, gradients, type ThemeColors } from './colors';
export { fontFamily, typeScale, type TypeVariant } from './typography';
export { spacing, borderRadius, iconSize, type Spacing, type BorderRadius, type IconSize } from './spacing';
export { shadows } from './shadows';
export { animationDuration, springConfigs, easingPresets, skeletonTiming } from './animations';
export { ThemeProvider, useTheme } from './ThemeProvider';
