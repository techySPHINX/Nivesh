apiVersion: v1
kind: Service
metadata:
  name: ml-api-svc
  namespace: nivesh
  labels:
    app: nivesh
    component: ml-api
spec:
  type: ClusterIP
  selector:
    app: nivesh
    component: ml-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
